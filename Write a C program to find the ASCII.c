//Write a C program to find the ASCII character of the given value.

#include <stdio.h>
int main()
{
    int num;
    scanf("%d",&num);
    printf("Character of ASCII Value %d is %c",num,num);
    return 0;
}
